<form method="post" action="{{ path('app_clearance_department_delete', {'id': clearance_department.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clearance_department.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
